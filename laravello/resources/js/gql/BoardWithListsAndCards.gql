query BoardWithListsAndCards($id: ID!) {
    board(id: $id) {
        id
        title
        color
        lists {
            id
            title
            board {
                id
            }
            cards {
                id
                title
                order
                owner {
                    id
                }
                list {
                    id
                }
            }
        }
    }
}
