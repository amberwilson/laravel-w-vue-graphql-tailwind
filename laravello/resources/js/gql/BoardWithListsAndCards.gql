query BoardWithListsAndCards($id: ID!) {
    board(id: $id) {
        id
        title
        lists {
            id
            title
            board {
                id
            }
            cards {
                id
                title
                order
            }
        }
    }
}
